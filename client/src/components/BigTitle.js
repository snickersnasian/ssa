import React from 'react'



export const BigTitle = (props) => {

    return (
        <div className="big-title">
            <h1>
                {props.title}
            </h1>
        </div>
    )
}